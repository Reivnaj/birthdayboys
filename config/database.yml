# PostgreSQL. Versions 8.2 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On OS X with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On OS X with MacPorts:
#   gem install pg -- --with-pg-config=/opt/local/lib/postgresql84/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.

development:
  adapter: postgresql
  encoding: unicode
  database: birthdayboy_development
  pool: 5
  
  timeout: 5000
 
 
test:
  adapter: postgresql
  encoding: unicode
  database: birthdayboy_test
  pool: 5

  timeout: 5000

production:
  adapter: postgresql
  encoding: unicode
  database: birthdayboy_production
  pool: 5

  timeout: 5000